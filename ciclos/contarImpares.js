function noParesDeContarImparesHasta(x) {
    let contadorimpares = 0;
    for (i = 1; i <= x; i += 2){
        contadorimpares += 1;
   }
   console.log(contadorimpares)
  }
  
  noParesDeContarImparesHasta(52)